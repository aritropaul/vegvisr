node_modules/
dist/
web/crates/worldgen/target/
.DS_Store
.vercel/
*.log
# Vite's dep pre-bundle cache; it lands wherever vite was invoked from.
.vite/

# web/src/wasm/ is intentionally NOT ignored: it is the wasm-bindgen output, and
# committing it means a web deploy needs only `vite build`, not a Rust toolchain.

# Apple build products
apple/build/
apple/*.xcodeproj/
apple/*.xcworkspace/
DerivedData/
