node_modules/
dist/
crates/worldgen/target/
.DS_Store
.vercel/
*.log

# src/wasm/ is intentionally NOT ignored: it is the wasm-bindgen output, and
# committing it means a deploy needs only `vite build`, not a Rust toolchain.
.vercel
